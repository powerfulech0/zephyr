name: E2E Tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main, 'feature/**']
  workflow_dispatch: # Allow manual trigger

# Cancel previous runs on same PR/branch
concurrency:
  group: e2e-${{ github.ref }}
  cancel-in-progress: true

jobs:
  e2e-tests:
    name: E2E Tests (Chromium, Firefox, WebKit)
    runs-on: ubuntu-latest
    timeout-minutes: 20

    # Service containers for PostgreSQL and Redis
    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_DB: zephyr_test
          POSTGRES_USER: zephyr_test
          POSTGRES_PASSWORD: zephyr_test_password
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

      redis:
        image: redis:7
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox, webkit]

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies (root)
        run: npm ci

      - name: Install backend dependencies
        working-directory: ./backend
        run: npm ci

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps ${{ matrix.browser }}

      - name: Run database migrations
        working-directory: ./backend
        run: npm run migrate:up || echo "No migrations to run"
        env:
          NODE_ENV: test
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: zephyr_test
          DB_USER: zephyr_test
          DB_PASSWORD: zephyr_test_password

      - name: Start backend server
        working-directory: ./backend
        run: npm start &
        env:
          NODE_ENV: test
          PORT: 4000
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: zephyr_test
          DB_USER: zephyr_test
          DB_PASSWORD: zephyr_test_password
          REDIS_HOST: localhost
          REDIS_PORT: 6379
          REDIS_PASSWORD: ""
          LOG_LEVEL: info
          ALLOWED_ORIGINS: http://localhost:5173

      - name: Start frontend dev server
        working-directory: ./frontend
        run: npm start &
        env:
          NODE_ENV: test
          PORT: 5173

      - name: Wait for services to be ready
        run: |
          npx wait-on http://localhost:4000/api/health http://localhost:5173 \
            --timeout 120000 \
            --interval 1000 \
            --verbose

      - name: Run E2E tests (${{ matrix.browser }})
        run: npm run test:e2e -- --project=${{ matrix.browser }}
        env:
          CI: true
          BASE_URL: http://localhost:5173
          API_URL: http://localhost:4000
          HEADLESS: true

      - name: Upload test artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-test-results-${{ matrix.browser }}
          path: |
            tests/e2e/reports/
            playwright-report/
            test-results/
          retention-days: 7

      - name: Upload HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-${{ matrix.browser }}
          path: playwright-report/
          retention-days: 7

      - name: Comment PR with test results
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs');
            const browser = '${{ matrix.browser }}';
            const status = '${{ job.status }}';

            let comment = `### E2E Test Results (${browser})\n\n`;
            comment += status === 'success'
              ? `✅ All E2E tests passed in ${browser}!`
              : `❌ E2E tests failed in ${browser}. Check artifacts for details.`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });

  e2e-tests-summary:
    name: E2E Tests Summary
    needs: e2e-tests
    runs-on: ubuntu-latest
    if: always()

    steps:
      - name: Check E2E test results
        run: |
          if [ "${{ needs.e2e-tests.result }}" == "success" ]; then
            echo "✅ All E2E tests passed across all browsers"
            exit 0
          else
            echo "❌ Some E2E tests failed. Check individual browser results above."
            exit 1
          fi

  # Optional: Run E2E tests on schedule (nightly)
  nightly-e2e:
    name: Nightly E2E Tests (Full Suite)
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule'
    timeout-minutes: 30

    # Service containers for PostgreSQL and Redis
    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_DB: zephyr_test
          POSTGRES_USER: zephyr_test
          POSTGRES_PASSWORD: zephyr_test_password
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

      redis:
        image: redis:7
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install all dependencies
        run: |
          npm ci
          cd backend && npm ci
          cd ../frontend && npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run database migrations
        working-directory: ./backend
        run: npm run migrate:up || echo "No migrations to run"
        env:
          NODE_ENV: test
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: zephyr_test
          DB_USER: zephyr_test
          DB_PASSWORD: zephyr_test_password

      - name: Start services
        run: |
          cd backend && npm start &
          cd ../frontend && npm start &
          npx wait-on http://localhost:4000/api/health http://localhost:5173
        env:
          NODE_ENV: test
          PORT: 4000
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: zephyr_test
          DB_USER: zephyr_test
          DB_PASSWORD: zephyr_test_password
          REDIS_HOST: localhost
          REDIS_PORT: 6379
          REDIS_PASSWORD: ""
          LOG_LEVEL: info
          ALLOWED_ORIGINS: http://localhost:5173

      - name: Run full E2E suite (all browsers, all tests)
        run: npm run test:e2e
        env:
          CI: true
          BASE_URL: http://localhost:5173
          API_URL: http://localhost:4000
          HEADLESS: true

      - name: Upload full test artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nightly-e2e-results
          path: |
            tests/e2e/reports/
            playwright-report/
            test-results/
          retention-days: 14
